<!DOCTYPE html>
<html>
<head>
  <title><%title%></title>
</head>
<body>
  <h1>Profiles List</h1>
  <ul>
    <% profiles.forEach(profile => { %>
      <li><a href="/profiles/<%= profile.id %>"><%= profile.name %></a></li>
    <% }); %>
  </ul>
</body>
</html>